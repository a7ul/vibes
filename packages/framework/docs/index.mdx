---
title: "Vibes Agent Framework"
description: "A TypeScript agent framework built to closely match pydantic-ai, powered by Vercel AI SDK."
---

Vibes is a TypeScript agent framework built to closely match [pydantic-ai](https://ai.pydantic.dev/), powered by the [Vercel AI SDK](https://sdk.vercel.ai/).

It brings production-grade agent patterns and the flexibility of pydantic-ai to TypeScript projects - type-safe tools, dependency injection, result validators, streaming, and first-class testing utilities.

```ts
import { Agent, tool } from "@vibes/framework";
import { anthropic } from "@ai-sdk/anthropic";
import { z } from "zod";

const weather = tool({
  name: "get_weather",
  description: "Get the current weather for a city",
  parameters: z.object({ city: z.string() }),
  execute: async (_ctx, { city }) => `${city}: 22°C, sunny`,
});

const agent = new Agent({
  model: anthropic("claude-haiku-4-5-20251001"),
  systemPrompt: "You are a helpful weather assistant.",
  tools: [weather],
});

const result = await agent.run("What's the weather in Tokyo?");
// "The weather in Tokyo is currently 22°C and sunny."
```

## How it fits together

```
Your code
   ↕
@vibes/framework   ← agent loop, tools, toolsets, DI, validation, testing
   ↕
Vercel AI SDK      ← models, streaming, structured output, providers
   ↕
Any LLM provider   ← Anthropic, OpenAI, Google, Mistral, Ollama, ...
```

## Start here

- [Installation](/getting-started/install) - set up Deno or Node.js and verify with a hello-world agent
- [Your First Agent](/getting-started/first-agent) - create an agent, run it, understand what each part does
- [Adding Tools](/getting-started/adding-tools) - give your agent real capabilities
- [Structured Output](/getting-started/structured-output) - get typed, validated JSON responses using Zod schemas

## pydantic-ai comparison

Vibes mirrors pydantic-ai closely. If you're porting a Python agent, most concepts transfer directly.

| Concept | pydantic-ai | @vibes/framework |
|---------|------------|-----------------|
| Core class | `Agent` | `Agent` |
| Run | `agent.run_sync()` | `agent.run()` |
| Streaming | `agent.run_stream()` | `agent.stream()` |
| Tools | `@agent.tool` decorator | `tool()` factory |
| Dependencies | `deps: TDeps` | `deps: TDeps` |
| Typed output | `result_type: MyModel` | `outputSchema: z.object(...)` |
| Type validation | Pydantic | Zod |
| Result validators | `@agent.result_validator` | `resultValidators: [...]` |
| Testing | `TestModel` / `FunctionModel` | `TestModel` / `FunctionModel` |
| Language | Python | TypeScript |
| Model layer | pydantic-ai providers | Vercel AI SDK |

See [Feature Parity](/reference/features) for the full comparison.
