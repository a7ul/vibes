---
phase: 01-landing-and-getting-started
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/getting-started/hello-world.mdx
autonomous: true
gap_closure: true
requirements:
  - GS-02

must_haves:
  truths:
    - "A developer can copy-paste the Step 4 test code verbatim and run it without a ReferenceError"
    - "The anthropic import is present in the Step 4 code block alongside the other imports"
  artifacts:
    - path: "docs/getting-started/hello-world.mdx"
      provides: "Step 4 test code block with all required imports"
      contains: "import { anthropic } from \"@ai-sdk/anthropic\""
  key_links:
    - from: "docs/getting-started/hello-world.mdx"
      to: "@ai-sdk/anthropic"
      via: "import statement in Step 4 code block"
      pattern: "import \\{ anthropic \\} from \"@ai-sdk/anthropic\""
---

<objective>
Fix the missing `anthropic` import in the Step 4 test code block of hello-world.mdx.

Purpose: The getting-started tutorial is the primary conversion path for new developers. A copy-paste failure in the final "test it" step damages the first impression Phase 1 is designed to create.
Output: A corrected hello-world.mdx where the Step 4 code block compiles and runs without errors.
</objective>

<execution_context>
@/Users/atul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/atul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing anthropic import to Step 4 test code block</name>
  <files>docs/getting-started/hello-world.mdx</files>
  <action>
In hello-world.mdx, locate the Step 4 test code block (the fenced ```ts block starting at approximately line 134 with "import { assertEquals } from "@std/assert"").

The import block at the top of that code block currently reads:
```
import { assertEquals } from "@std/assert";
import { Agent, tool, TestModel, setAllowModelRequests } from "@vibes/framework";
import { z } from "zod";
```

Add `import { anthropic } from "@ai-sdk/anthropic";` as the fourth import line, immediately after the `import { z } from "zod";` line. The resulting import block should be:
```
import { assertEquals } from "@std/assert";
import { Agent, tool, TestModel, setAllowModelRequests } from "@vibes/framework";
import { z } from "zod";
import { anthropic } from "@ai-sdk/anthropic";
```

No other changes to this file. Steps 1, 2, and 3 already have correct imports — do not touch them.
  </action>
  <verify>
    <automated>grep -n 'import { anthropic }' /Users/atul/Projects/personal/vibes/packages/framework/docs/getting-started/hello-world.mdx</automated>
  </verify>
  <done>The Step 4 code block contains `import { anthropic } from "@ai-sdk/anthropic"` and uses `anthropic("claude-haiku-4-5-20251001")` later in the same code block without any undefined reference. The import appears exactly once inside the Step 4 fenced code block.</done>
</task>

</tasks>

<verification>
After the fix:
- `grep -n 'import { anthropic }' docs/getting-started/hello-world.mdx` returns at least one match inside the Step 4 code block
- `grep -n 'anthropic(' docs/getting-started/hello-world.mdx` and cross-reference: every occurrence of `anthropic(` in a code block has a corresponding `import { anthropic }` in the same code block
- The file line count is 207 (206 original + 1 added line)
</verification>

<success_criteria>
A developer can copy the entire Step 4 code block from hello-world.mdx, save it as `agent_test.ts`, and run `deno test agent_test.ts` without a `ReferenceError: anthropic is not defined` error. The import is present and the code is self-contained.
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-and-getting-started/01-03-SUMMARY.md`
</output>
