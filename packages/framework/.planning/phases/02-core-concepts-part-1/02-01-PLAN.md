---
phase: 02-core-concepts-part-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/concepts/agents.mdx
  - docs/concepts/models.mdx
  - docs/concepts/dependencies.mdx
  - docs/concepts/tools.mdx
autonomous: true
requirements:
  - CONCEPT-01
  - CONCEPT-02
  - CONCEPT-03
  - CONCEPT-04

must_haves:
  truths:
    - "A developer can navigate to /concepts/agents and read a full agent loop explanation with a Mermaid flowchart"
    - "A developer can navigate to /concepts/models and find quickstart snippets for all 7 supported providers"
    - "A developer can navigate to /concepts/dependencies and see the RunContext fan-out diagram with correct 7-field interface"
    - "A developer can navigate to /concepts/tools and read about all 4 tool factories with the execution pipeline diagram"
    - "Every code example uses exports verified against mod.ts - no invented APIs, no event.type bugs"
  artifacts:
    - path: "docs/concepts/agents.mdx"
      provides: "Agent class deep dive with agent loop Mermaid flowchart"
      contains: "mermaid"
    - path: "docs/concepts/models.mdx"
      provides: "Models page with 7 provider quickstarts and ModelSettings table"
      contains: "ModelSettings"
    - path: "docs/concepts/dependencies.mdx"
      provides: "RunContext DI page with fan-out Mermaid diagram and correct 7-field interface"
      contains: "RunContext"
    - path: "docs/concepts/tools.mdx"
      provides: "Tools page covering all 4 factories with execution pipeline Mermaid"
      contains: "outputTool"
  key_links:
    - from: "docs/concepts/agents.mdx"
      to: "lib/agent.ts AgentOptions"
      via: "All AgentOptions fields documented in reference table"
    - from: "docs/concepts/dependencies.mdx"
      to: "lib/types/context.ts RunContext"
      via: "Full 7-field + attachMetadata() interface documented"
    - from: "docs/concepts/tools.mdx"
      to: "lib/tool.ts tool/plainTool/outputTool/fromSchema"
      via: "All 4 factories with execute signature examples"
---

<objective>
Create four deep-dive concept pages - agents, models, dependencies, and tools - that form the foundation of the Vibes documentation.

Purpose: Developers who complete the hello-world tutorial need a place to go deeper on each abstraction. These four pages cover the most fundamental building blocks of any Vibes agent.

Output: Four new MDX files under docs/concepts/ following the Pydantic AI teaching style: explain the concept, show a Mermaid diagram, walk through each API feature with verified code examples, close with a reference table.
</objective>

<execution_context>
@/Users/atul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/atul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-concepts-part-1/02-RESEARCH.md

<!-- Key source files to verify examples against - do NOT invent APIs -->
@lib/agent.ts
@lib/tool.ts
@lib/types/context.ts
@lib/types/model_settings.ts
@lib/types/output_mode.ts
@mod.ts

<!-- Existing docs to reuse content from (not replace yet) -->
@docs/concepts/how-agents-work.mdx
@docs/concepts/dependency-injection.mdx
@docs/reference/core/tools.mdx
@docs/getting-started/install.mdx
</context>

<interfaces>
<!-- Verified exports from mod.ts that must appear in examples -->
<!-- Source: 02-RESEARCH.md Standard Stack table -->

Agent, tool, plainTool, outputTool, fromSchema - all exported from "@vibes/framework"
z (Zod) - exported from "@vibes/framework"
RunContext<TDeps>, ModelSettings, OutputMode - type exports from "@vibes/framework"

AgentOptions fields (from lib/agent.ts):
  name?, model (required), systemPrompt, instructions, tools, toolsets,
  outputSchema, outputMode ('tool'|'native'|'prompted'), outputTemplate (boolean, NOT string),
  resultValidators, maxRetries (default 3), maxTurns (default 10),
  usageLimits, historyProcessors, modelSettings, endStrategy ('early'|'exhaustive'),
  maxConcurrency, telemetry

RunContext<TDeps> fields (from lib/types/context.ts) - FULL 7-field interface:
  deps: TDeps
  usage: Usage
  retryCount: number
  toolName: string | null
  runId: string
  metadata: Record<string, unknown>
  toolResultMetadata: Map<string, Record<string, unknown>>
  attachMetadata(toolCallId: string, meta: Record<string, unknown>): void

tool() factory signature (from lib/tool.ts):
  tool<TDeps>({ name, description, parameters (ZodType), execute(ctx, args),
    argsValidator?, prepare?, requiresApproval?, sequential?, maxRetries? })

plainTool() - no RunContext, execute receives args directly
outputTool() - terminal tool, output becomes agent result
fromSchema() - uses raw JSON Schema instead of Zod, parameters: jsonSchema field
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create agents.mdx - Agent class deep dive</name>
  <files>docs/concepts/agents.mdx</files>
  <action>
Create docs/concepts/agents.mdx as a teaching-style concept page. The page must:

1. Frontmatter: title "Agents", description "The Agent class is the central abstraction in Vibes..."

2. Opening (2 sentences): What an Agent is and why it exists. Note it wraps the Vercel AI SDK model loop.

3. Mermaid flowchart - paste verbatim from 02-RESEARCH.md "CONCEPT-01: Agent Loop Flowchart":
   The flowchart shows: agent.run/stream/runStreamEvents → createRunContext → resolve prompts → Turn loop → prepareTurn → generateText (Vercel AI SDK) → Response type branch (text only → RunResult | final_result tool → parse → validate → retry or MaxRetriesError | tool calls → Execute tools → requiresApproval → ApprovalRequiredError or append results → maxTurns check → MaxTurnsError or loop back)

4. Section "Basic Usage" - minimal agent example (import Agent from @vibes/framework, import anthropic from @ai-sdk/anthropic, create agent with model + systemPrompt, await agent.run)

5. Section "Type Parameters" - explain Agent<TDeps, TOutput>. Show the Deps type example from RESEARCH.md (Agent<Deps, string> with systemPrompt as a function using ctx.deps).

6. Section "Constructor Options" - AgentOptions reference table with ALL fields from the interfaces block above (name, model, systemPrompt, instructions, tools, toolsets, outputSchema, outputMode, outputTemplate, resultValidators, maxRetries, maxTurns, usageLimits, historyProcessors, modelSettings, endStrategy, maxConcurrency, telemetry). Include Type and Default columns.

7. Section "Running an Agent" - show agent.run(), agent.stream(), agent.runStreamEvents() signatures briefly. Note RunResult vs StreamResult (link to concepts/results and concepts/streaming).

8. Section "agent.override()" - explain per-run overrides for testing and dynamic config. Show the override example from RESEARCH.md (agent.override({ model: testModel, maxTurns: 3 }).run(...)).

9. Mintlify Info callout: "Tip: Use agent.override() in tests to swap in a TestModel without modifying your production agent."

10. Closing cards: link to concepts/models, concepts/dependencies, concepts/tools.

CRITICAL: outputTemplate is boolean (not string). Do NOT document it as accepting a template string. Default is true.
  </action>
  <verify>
    <automated>test -f /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/agents.mdx && grep -l "mermaid" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/agents.mdx && grep -l "agent.override" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/agents.mdx</automated>
  </verify>
  <done>docs/concepts/agents.mdx exists, contains a mermaid block, documents agent.override(), and lists all AgentOptions fields in a reference table.</done>
</task>

<task type="auto">
  <name>Task 2: Create models.mdx - Vercel AI SDK model layer</name>
  <files>docs/concepts/models.mdx</files>
  <action>
Create docs/concepts/models.mdx as a teaching-style concept page. The page must:

1. Frontmatter: title "Models", description "Vibes uses the Vercel AI SDK model abstraction..."

2. Opening: Explain that Vibes accepts any Vercel AI SDK LanguageModel. The model is passed at agent construction and can be overridden per-run. Do NOT duplicate the install page setup steps - link to getting-started/install instead.

3. Mermaid diagram - simple layered architecture showing: App → Agent (Vibes) → LanguageModel interface (Vercel AI SDK) → Provider adapters (Anthropic, OpenAI, Google, Groq, Mistral, Ollama, OpenAI-compatible) → LLM APIs. Use a graph TD.

4. Section "Provider Quickstarts" - subsection for each of the 7 providers. Use code examples from RESEARCH.md:
   - Anthropic: @ai-sdk/anthropic, ANTHROPIC_API_KEY, anthropic("claude-sonnet-4-6")
   - OpenAI: @ai-sdk/openai, OPENAI_API_KEY, openai("gpt-4o")
   - Google Gemini: @ai-sdk/google, GOOGLE_GENERATIVE_AI_API_KEY, google("gemini-2.0-flash")
   - Groq: @ai-sdk/groq, GROQ_API_KEY, groq("llama-3.3-70b-versatile")
   - Mistral: @ai-sdk/mistral, MISTRAL_API_KEY, mistral("mistral-large-latest")
   - Ollama: ollama-ai-provider, no API key needed, ollama("llama3.2")
   - OpenAI-compatible: createOpenAI with baseURL from RESEARCH.md CONCEPT-02 example

5. Section "ModelSettings" - explain the ModelSettings type. Show reference table: temperature (number?), maxTokens (number?), topP (number?), topK (number?), presencePenalty (number?), frequencyPenalty (number?), stopSequences (string[]?), seed (number?). Show usage: new Agent({ model, modelSettings: { temperature: 0.7, maxTokens: 2000 } }).

6. Section "Overriding the Model Per Run" - show agent.override({ model: cheapModel }).run(prompt) for dynamic model selection. Mention use case: use a cheaper model for classification, expensive model for generation.

7. Mintlify Info callout: "All provider packages follow the Vercel AI SDK spec. Install the provider package for your chosen model, set the API key env var, and pass the model instance to Agent."

8. Closing cards: link to getting-started/install, concepts/agents.
  </action>
  <verify>
    <automated>test -f /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/models.mdx && grep -l "ModelSettings" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/models.mdx && grep -l "mermaid" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/models.mdx</automated>
  </verify>
  <done>docs/concepts/models.mdx exists, contains a Mermaid diagram, documents all 7 provider quickstarts, and has a ModelSettings reference table.</done>
</task>

<task type="auto">
  <name>Task 3: Create dependencies.mdx and tools.mdx</name>
  <files>docs/concepts/dependencies.mdx, docs/concepts/tools.mdx</files>
  <action>
Create two MDX files in the same task since they are both medium-length pages with clear requirements.

--- docs/concepts/dependencies.mdx ---

1. Frontmatter: title "Dependencies", description "RunContext is Vibes's dependency injection system..."

2. Opening: Explain RunContext<TDeps> as the primary DI mechanism. The agent is typed with its dependencies at construction time; deps are injected at agent.run() time. This is a signature feature: tools, prompts, validators, and toolsets all receive the same context.

3. Mermaid diagram - paste verbatim from RESEARCH.md "CONCEPT-03: RunContext Fan-Out Diagram":
   Shows agent.run/stream → RunContext<TDeps> → fan out to: systemPrompt(ctx), instructions(ctx), tool.execute(ctx, args), tool.prepare(ctx), tool.requiresApproval(ctx, args), resultValidators(ctx, output), toolset.tools(ctx), historyProcessors(messages, ctx)

4. Section "Defining Dependencies" - show type Deps = { db: Database }. Create Agent<Deps>. Pass deps at run time: agent.run("...", { deps: { db: myDb } }).

5. Section "The RunContext Interface" - show the FULL correct interface from the interfaces block:
   deps, usage, retryCount, toolName (string|null), runId, metadata, toolResultMetadata, attachMetadata().
   Reference table with all 7 fields + method. DO NOT omit retryCount, toolName, toolResultMetadata, attachMetadata.

6. Section "Using Deps in Tools" - show the tool() factory example from RESEARCH.md CONCEPT-04 section (search tool using ctx.deps.db.search).

7. Section "Using Deps in System Prompts" - show systemPrompt as function: (ctx) => `Helping from ${ctx.deps.db.region}`.

8. Section "Testing with Dependencies" - show creating a fakeDeps object and passing it to agent.override().run(). Mintlify Info callout: "This is the recommended testing pattern - inject test doubles via deps, never mock the framework internals."

9. Section "History Processors and Privacy" - brief intro to privacyFilterProcessor. Show the CORRECT PrivacyRule shapes from RESEARCH.md CONCEPT-07 section:
   { pattern: /\d{4}-\d{4}-\d{4}-\d{4}/g, replacement: "[CARD]" } - RegexPrivacyRule
   { messageType: "tool", fieldPath: "content.0.result.ssn" } - FieldPrivacyRule
   CRITICAL: Do NOT use { type: "regex", pattern: ..., redactValue: ... } - that is the WRONG shape from the old docs.

10. Closing cards: link to concepts/tools, concepts/agents.

--- docs/concepts/tools.mdx ---

1. Frontmatter: title "Tools", description "Tools are functions the agent can call during a run..."

2. Opening: Explain tools as the primary way agents take actions. The model decides when to call tools; Vibes executes them and feeds results back.

3. Mermaid diagram - paste verbatim from RESEARCH.md "CONCEPT-04: Tool Execution Pipeline":
   Shows: Model returns tool call → Look up tool → Validate args (Zod) → argsValidator → requiresApproval → execute → isOutput → final output or append to history

4. Section "tool() - Tools with Dependencies" - full example from RESEARCH.md CONCEPT-04 tool() section. Show all options: name, description, parameters (Zod), execute(ctx, args), argsValidator, prepare, requiresApproval, sequential, maxRetries.

5. Section "plainTool() - Simple Tools" - plainTool example from RESEARCH.md (add tool with a + b). Explain: no RunContext, just args. Use when the tool has no side effects and needs no deps.

6. Section "outputTool() - Terminal Tools" - outputTool example from RESEARCH.md (done tool). Explain: when the model calls this tool, the run ends and the tool's return value becomes the agent output. Use with outputMode: 'tool'.

7. Section "fromSchema() - Raw JSON Schema Tools" - fromSchema example from RESEARCH.md. Explain: use when you have a pre-existing JSON Schema or when Zod is overkill. No type inference available.

8. Section "Tool Options Reference" - table of all tool() options: name (string), description (string), parameters (ZodType), execute (function), argsValidator (function?), prepare (function?), requiresApproval (boolean|function?), sequential (boolean?), maxRetries (number?).

9. Section "prepare - Conditional Tool Availability" - explain that prepare returning null removes the tool from the available set for that turn. Show the ctx.deps.db.isConnected() ? undefined : null example.

10. Section "Multi-modal Returns" - brief note: tools can return strings, objects, or multi-modal content (images, files). The result is serialized into the message history.

11. Closing cards: link to concepts/toolsets, concepts/dependencies.
  </action>
  <verify>
    <automated>test -f /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/dependencies.mdx && test -f /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/tools.mdx && grep -l "mermaid" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/tools.mdx && grep "outputTool" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/tools.mdx</automated>
  </verify>
  <done>Both docs/concepts/dependencies.mdx and docs/concepts/tools.mdx exist. dependencies.mdx has the RunContext fan-out Mermaid diagram and correct PrivacyRule shapes (no "type: regex"). tools.mdx has the execution pipeline Mermaid diagram and documents all 4 tool factories.</done>
</task>

</tasks>

<verification>
After all tasks:
1. Run: `ls /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/*.mdx` - should show agents.mdx, models.mdx, dependencies.mdx, tools.mdx (alongside existing how-agents-work.mdx, dependency-injection.mdx, error-handling.mdx)
2. Run: `grep -l "mermaid" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/*.mdx` - all 4 new files should appear
3. Run: `grep "event\.type" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/*.mdx` - should return nothing (no incorrect event.type usage)
4. Run: `grep "outputTemplate" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/agents.mdx` - should show boolean usage, not string
5. Run: `grep "type: .regex" /Users/atul/Projects/personal/vibes/packages/framework/docs/concepts/dependencies.mdx` - should return nothing (no incorrect PrivacyRule shape)
</verification>

<success_criteria>
- docs/concepts/agents.mdx exists with agent loop Mermaid flowchart, full AgentOptions table, and agent.override() example
- docs/concepts/models.mdx exists with provider architecture diagram and quickstarts for all 7 providers plus ModelSettings table
- docs/concepts/dependencies.mdx exists with RunContext fan-out diagram and CORRECT full 7-field interface (no omissions, no wrong PrivacyRule shape)
- docs/concepts/tools.mdx exists with execution pipeline diagram and all 4 tool factories documented
- Zero occurrences of wrong APIs: no event.type, no outputTemplate as string, no PrivacyRule with type: "regex" key
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-concepts-part-1/02-01-SUMMARY.md`
</output>
